---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---
<header class="page-header">
  <div class="container">
    <h1>{title}</h1>
    {subtitle && <p>{subtitle}</p>}
  </div>
</header>
